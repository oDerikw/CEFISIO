export interface Product {
  id: number,
  name: string,
  description: string,
  monthlyPrice: number,
  yearlyPrice: number,
  enabled: true,
  maxProcess: number,
  maxCompanies: number,
  maxUsers: number,
}
